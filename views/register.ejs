<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/registerstyle.css">

</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <% if (error && error.length > 0) { %>
        <div class="error-box">
            <span class="error-text">
                <%= error %>
            </span>
        </div>
    <% } %>
    <div class="absolute top-4 left-4">
        <h1 class="text-3xl font-bold logo">
            <span class="chro">Chro</span><span class="sneaks">sneaks</span>
        </h1>
    </div>
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
        <h2 class="text-2xl font-bold mb-6 text-center text-indigo-600">Register</h2>
        <form method="post" action="/users/register">
            <div class="mb-4">
                <label class="block text-gray-700 mb-2" name="fullname">Full Name</label>
                <input class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring focus:ring-indigo-200" type="text" name="fullname" placeholder="John Doe">
            </div>
            <div class="mb-4">
                <label class="block text-gray-700 mb-2">Email Address</label>
                <input class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring focus:ring-indigo-200" type="email" name="email" placeholder="john@example.com">
            </div>
            <div class="mb-4">
                <label class="block text-gray-700 mb-2 ">Password</label>
                <input class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring focus:ring-indigo-200" type="password" name="password" placeholder="********">
            </div>
            <button class="w-full bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700 transition duration-200" type="submit">Register</button>
        </form>
        <p class="mt-4 text-center">Already have an account? <a href="/login" class="login-link">Login here</a></p>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const errorBox = document.querySelector('.error-box');
            if (errorBox) {
                setTimeout(function() {
                    errorBox.classList.add('hidden');
                }, 3000); // Time in milliseconds (3 seconds here)
            }
        });
    </script>
</body>
</html>
