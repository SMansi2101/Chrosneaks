<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
            .header-container {
        font-family: 'Montserrat', sans-serif;
        background-color: #f9fafb; /* Tailwind's gray-100 */
        padding: 0.5rem 1rem;
        border-radius: 0.375rem; /* Tailwind's rounded-md */
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 20; /* Ensure it stays on top */
    }

    .header-link {
        color: #4b5563; /* Tailwind's gray-600 */
        transition: color 0.3s ease, background-color 0.3s ease;
        padding: 0.25rem 0.5rem;
        border-radius: 0.375rem; /* Tailwind's rounded-md */
        font-size: 1rem;
    }

    .header-link:hover {
        background-color: #e5e7eb; /* Tailwind's gray-200 */
        color: #1f2937; /* Tailwind's gray-800 */
    }
    .logout-link {
        color: #bf1c1c; /* Tailwind's red-500 */
    }

    </style>
</head>
<body>
    <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
    <% if(loggedin) %>
  <%  {  %>
    <div class="header-container fixed top-0 right-0 mt-2 mr-4 flex space-x-4">
        <a href="/shop" class="header-link font-bold">Shop</a>
        <a href="/users/cart" class="header-link font-bold">Cart</a>
        <a href="" class="header-link font-bold">My Account</a>
        <form action="/users/logout" method="POST" style="display: inline;">
            <button type="submit" class="header-link logout-link text-red-600 font-bold" style="background: none; border: none; cursor: pointer;">Logout</button>
        </form>
    </div>
   <% }  %>
</body>
</html>